version: "3.9"

services:
  <%= config[:service] %>:
    container_name: <%= config[:service] %>
    build: "./"
    platform: "linux/x86_64"
    networks:
      - default
    restart: unless-stopped
networks:
  default:
    external: true
    name: ${NETWORK_NAME}
